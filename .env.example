# 复制成 .env。只有「读配置本身需要的东西」走 env，其余一切以数据库为真相（见 spec Q32）。
# 监听地址与端口在数据库的 server 配置里改，不在这儿 —— 改完刷新页面即生效，不用重启。
DATA_DIR=./data
MASTER_KEY_PATH=./data/master.key

# 有 MASTER_KEY 时用它派生，不落 key 文件（容器里常这么干）。至少 16 位。
# MASTER_KEY=

# 容器里设成 1，日志走结构化 JSON 由 docker logs 收走；本地留空，用 pino-pretty。
# LOG_JSON=1
# LOG_LEVEL=info
