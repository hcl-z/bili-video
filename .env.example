# 复制成 .env。只有「读配置本身需要的数据」走 env，其余一切以数据库为真相（见 spec Q32）
# 监听地址与端口在数据库的 server 配置里改，不在此处 —— 改完刷新页面即生效，不用重启
DATA_DIR=./data
MASTER_KEY_PATH=./data/master.key




# 容器里设成 1，日志走结构化 JSON 由 docker logs 收走；本地留空，用 pino-pretty
# LOG_JSON=1
